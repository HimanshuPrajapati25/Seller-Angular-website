import { SuggestionSearchPipe } from './suggestion-search.pipe';

describe('SuggestionSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SuggestionSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
